# My-leetcode-journey
"Given an integer array nums and an integer val, remove all occurrences of val in nums in-place. Using Python list comprehension with slice assignment (nums[:]), we filter out val efficiently without changing the memory address. This satisfies the O(1) space constraint and returns the new length."
